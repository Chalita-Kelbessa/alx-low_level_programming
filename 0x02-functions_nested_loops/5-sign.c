#include "main.h"

/**
 * print_sign - To putch sign of a number
 *
 * @n: any number
 * Return: 0 or 1
 */

int print_sign(int n)
{
while ((n) > 0)
	{
	return (43 && 49);
	_putchar(43);
	n++;
	}
while ((n) < 0)
	{
	return (45 && 49);
	_putchar(45);
	n--;
	}
while (n == 0)
	{
	return (48 && 48);
	_putchar(48);
	}
return (0);
}
