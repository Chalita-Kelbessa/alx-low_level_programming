#include "main.h"

/**
 * jack_bauer - prints every minute of  the day
 *
 * Return 0
 *
 */

void jack_bauer(void)
{
	a = 58;
	b = 58;
	c = 58;
	d = 58;

	while (a <= 50)
	{
		b = 48;
		while (b <= 54)
		_putchar (a);
		_putchar (b);
	_putchar ('\n');
	return (0);
}
