#include "main.h"

/**
 * jack_bauer - prints every minute of  the day
 *
 * Return: void
 *
 */

void jack_bauer(void)
{
	int m, n, s, t;

	for (m = 48; m <= 50; m++)
	{
		for (n = 48; n <= 57; n++)
		{
			for (s = 48; s <= 53; s++)
			{
				for (t = 48; t <= 57; t++)
				{
					if (m >= 50 && n >= 52
						break;
					_putchar(m);
					_putchar(n);
					_putchar(58);
					_putchar(s);
					_putchar(t);
					_putchar('\n');

				}
			}
		}
	}
}

