#include "main.h"

/**
 * _isupper - outputs 1 if c is upper case
 *
 * @c: alphabet in upper case
 * Return: 1 success
 *
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)

		return (1);

	else

		return (0);
}
